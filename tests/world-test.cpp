#include <gtest/gtest.h>

TEST(WorldTest, addBody) {
    EXPECT_EQ(1, 1);
    EXPECT_TRUE(true);
}

TEST(WorldTest, addJoint) {
    EXPECT_EQ(1, 1);
    EXPECT_TRUE(true);
}

TEST(WorldTest, clear) {
    EXPECT_EQ(1, 1);
    EXPECT_TRUE(true);
}

TEST(WorldTest, step) {
    EXPECT_EQ(1, 1);
    EXPECT_TRUE(true);
}