#include <gtest/gtest.h>

TEST(CollisionTest, posA) {
    EXPECT_EQ(1, 1);
    EXPECT_TRUE(true);
}

TEST(CollisionTest, posB) {
    EXPECT_EQ(1, 1);
    EXPECT_TRUE(true);
}

TEST(CollisionTest, posC) {
    EXPECT_EQ(1, 1);
    EXPECT_TRUE(true);
}