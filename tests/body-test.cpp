#include "gtest/gtest.h"

TEST(BodyTest, torque) {
    EXPECT_EQ(1, 1);
    EXPECT_TRUE(true);
}

TEST(BodyTest, velocity) {
    EXPECT_EQ(1, 1);
    EXPECT_TRUE(true); 
}

TEST(BodyTest, rotation) {
    EXPECT_EQ(1, 1);
    EXPECT_TRUE(true);
}